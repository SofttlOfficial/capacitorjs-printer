import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.softtl.capacitorjs.printer',
  appName: 'capacitorjs-printer',
  webDir: 'www'
};

export default config;
